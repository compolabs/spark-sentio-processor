{
  "private": true,
  "name": "spark-processor",
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "build": "sentio build",
    "gen": "sentio gen",
    "test": "sentio test",
    "upload": "sentio upload",
    "postinstall": "sentio gen"
  },
  "dependencies": {
    "@sentio/sdk": "^2.53.2"
  },
  "devDependencies": {
    "@sentio/cli": "^2.23.3",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.2"
  }
}
